# API server environment
# Do NOT commit your real .env with secrets to git.
# Example values:
PORT=3001
API_ALLOWED_ORIGIN=http://localhost:5173
SESSION_TTL_MS=2592000000 # 30 days in ms
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=120
# If you will proxy AI provider calls through the server, set the provider key here (server-only):
GOOGLE_API_KEY=

# Cookie settings (set COOKIE_SECURE=true when serving over HTTPS)
COOKIE_SECURE=false
COOKIE_SAMESITE=lax

# Path to sqlite DB (optional)
DATABASE_FILE=./database.db
